ingress:
  enabled: true
  path: /
  hosts:
    - grafana.home
    - grafana.home.vpn

adminPassword: admin
image:
  tag: 8.0.0

persistence:
  enabled: true
  storageClassName: local-path
  size: 2Gi
